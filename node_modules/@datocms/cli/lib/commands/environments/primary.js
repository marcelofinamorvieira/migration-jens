"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const cli_utils_1 = require("@datocms/cli-utils");
class Command extends cli_utils_1.CmaClientCommand {
    async run() {
        const environments = await this.client.environments.list();
        const primary = environments.find((e) => e.meta.primary);
        this.log(primary.id);
        return primary;
    }
}
exports.default = Command;
Command.description = 'Returns the name the primary environment of a project';
Command.flags = {
    ...cli_utils_1.CmaClientCommand.flags,
};
