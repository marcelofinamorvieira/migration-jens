"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const cli_utils_1 = require("@datocms/cli-utils");
class Command extends cli_utils_1.BaseCommand {
    async run() {
        const installedPlugins = this.config.plugins;
        const maybeInstalled = Command.availablePlugins.map((p) => ({
            ...p,
            installed: installedPlugins.some((ip) => ip.name === p.package),
        }));
        this.printTable(maybeInstalled, ['package', 'description', 'installed'], []);
        return maybeInstalled;
    }
}
exports.default = Command;
Command.description = 'Lists official DatoCMS CLI plugins';
Command.flags = {
    ...cli_utils_1.BaseCommand.flags,
    ...cli_utils_1.oclif.CliUx.ux.table.flags(),
};
Command.availablePlugins = [
    {
        package: '@datocms/cli-plugin-wordpress',
        description: 'Import a WordPress site into DatoCMS',
    },
];
