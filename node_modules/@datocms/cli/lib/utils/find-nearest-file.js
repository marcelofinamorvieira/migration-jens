"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findNearestFile = void 0;
const path_1 = require("path");
const promises_1 = require("fs/promises");
async function findNearestFile(fileName, directoryPath = (0, path_1.resolve)()) {
    try {
        const path = (0, path_1.join)(directoryPath, fileName);
        await (0, promises_1.access)(path);
        return path;
    }
    catch {
        const parentDirectoryPath = (0, path_1.dirname)(directoryPath);
        if (parentDirectoryPath === directoryPath) {
            throw new Error(`No "${fileName}" file found`);
        }
        return findNearestFile(fileName, parentDirectoryPath);
    }
}
exports.findNearestFile = findNearestFile;
