import { Config } from './config';
import { BaseCommand } from './base-command';
export declare abstract class DatoConfigCommand<T extends typeof DatoConfigCommand.flags> extends BaseCommand<T> {
    static flags: {
        'config-file': import("@oclif/core/lib/interfaces").OptionFlag<string>;
    };
    protected datoConfigPath: string;
    protected datoConfigRelativePath: string;
    protected datoConfig?: Config;
    protected init(): Promise<void>;
    protected requireDatoConfig(): void;
    protected saveDatoConfig(config: Config): Promise<void>;
}
